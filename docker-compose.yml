version: '3.8'

services:
  optitrade:
    build: .
    image: optitrade-engine:latest
    privileged: true
    volumes:
      - /dev/hugepages:/dev/hugepages
    command: ["./build-dpdk/apps/optitrade_hello"]
